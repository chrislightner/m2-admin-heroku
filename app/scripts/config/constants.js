// angular.module('Constants', [])

// .constant('Constants', {API_URL:'https://coveragedetails.net/api/index.php/'})

// ;







//No longer in use. The frontend and admin apps no access an external config file to make development work correctly
//The "app/m2-config" exists so local grunt serve works correctly, but is not used by the live apps
